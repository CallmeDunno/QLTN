@using BTL.Models;
@using X.PagedList.Mvc.Core;
@model X.PagedList.IPagedList<LoaiNha>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "ListLoaiNha";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Thông tin loại nhà</h1>
<p>@TempData["Message"]</p>
<p>
    <a class="btn btn-primary" asp-action="AddLoaiNha">Thêm mới</a>
</p>
<div class="table-responsive">
    <table class="table">
        <thead>
            <tr class="table-danger">
                <th>
                    @Html.DisplayNameFor(model => model[0].TenLoai)
                </th>
                <th>
                    Hành động
                </th>
            </tr>
        </thead>
        <tbody>
            @{
                int i = 1;
            }
            @foreach (var item in Model)
            {
                i = i + 1;
                string cls = "table-info";
                if (i % 2 == 0)
                {
                    cls = "table-success";
                }
                <tr class="@cls">
                    <td>
                        @Html.DisplayFor(modelItem => item.TenLoai)
                    </td>
                    <td>
                        <a asp-action="UpdateLoaiNha" asp-route-id="@item.MaLoai">Sửa</a> |
                        <a asp-action="DeleteLoaiNha" asp-route-id="@item.MaLoai">Xóa</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <div class="product__pagination">
        @Html.PagedListPager(Model, page=>Url.Action("ThongTinNha", new{page=page}))
    </div>
</div>
<style>
    .pagination li.active {
        width: 30px;
        height: 30px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        background: #b66dff;
        border-color: #b66dff;
        color: #ffffff;
        font-weight: bold;
    }

    .pagination li {
        width: 30px;
        height: 30px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        background: #ffffff;
        border-color: #ffffff;
        color: #000000;
        font-weight: bold;
    }
</style>
