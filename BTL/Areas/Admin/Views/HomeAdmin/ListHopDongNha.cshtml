@using BTL.Models;
@using X.PagedList.Mvc.Core;
@model X.PagedList.IPagedList<HopDongNha>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "ListHopDongNha";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Thông tin hợp đồng nhà</h1>
<p>@TempData["Message"]</p>
<p>
    <a class="btn btn-primary" asp-action="AddHopDongNha">Thêm mới</a>
</p>
<div class="table-responsive">
    <table class="table">
        <thead>
            <tr class="table-danger">
                <th>
                    @Html.DisplayNameFor(model => model[0].ThoiGianBatDau)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model[0].ThoiGianKetThuc)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model[0].IsDeleted)
                </th>
                <th>Hành động</th>
            </tr>
        </thead>
        <tbody>
            @{
                int i = 1;
            }
            @foreach (var item in Model)
            {
                i = i + 1;
                string cls = "table-info";
                if (i % 2 == 0)
                {
                    cls = "table-success";
                }
                <tr class="@cls">
                    <td>
                        @Html.DisplayFor(modelItem => item.ThoiGianBatDau)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ThoiGianKetThuc)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IsDeleted)
                    </td>
                    <td>
                        <a asp-action="UpdateHopDongNha" asp-route-id="@item.MaHopDong">Sửa</a> |
                        <a asp-action="DeleteHopDongNha" asp-route-id="@item.MaHopDong">Xóa</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <div class="product__pagination">
        @Html.PagedListPager(Model, page=>Url.Action("ThongTinNha", new{page=page}))
    </div>
</div>
<style>
    .pagination li.active {
        width: 30px;
        height: 30px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        background: #b66dff;
        border-color: #b66dff;
        color: #ffffff;
        font-weight: bold;
    }

    .pagination li {
        width: 30px;
        height: 30px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        background: #ffffff;
        border-color: #ffffff;
        color: #000000;
        font-weight: bold;
    }
</style>


